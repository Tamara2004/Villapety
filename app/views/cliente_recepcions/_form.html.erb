<%= form_for(@cliente_recepcion) do |f| %>
  <% if @cliente_recepcion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente_recepcion.errors.count, "error") %> prohibited this cliente_recepcion from being saved:</h2>

      <ul>
      <% @cliente_recepcion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cliente_id %><br>
      <%= f.select( "cliente_id",Cliente.all.collect{|l| [ l.nombre, l.id ] }, {include_blank: 'Elija Cliente'}) %>
  </div>
  <div class="field">
    <%= f.label :recepcion_id %><br>
      <%= f.select( "recepcion_id",Recepcion.all.collect{|l| [ l.nombre, l.id ] }, {include_blank: 'Elija Recepcion'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
